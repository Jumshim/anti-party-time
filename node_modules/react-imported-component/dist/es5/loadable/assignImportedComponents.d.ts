import { Promised } from '../types';
declare type ImportedDefinition = [Promised<any>, string, string, boolean];
/**
 * to be used __only via CLI tools__
 */
export declare const assignImportedComponents: (set: ImportedDefinition[]) => ImportedDefinition[];
export {};
