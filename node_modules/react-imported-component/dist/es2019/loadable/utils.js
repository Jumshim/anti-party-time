import { settings } from '../configuration/config';
export const toKnownSignature = (signature, marks) => (!settings.checkSignatures && marks.join('|')) || signature;
export const markerOverlap = (a1, a2) => a1.filter((mark) => a2.indexOf(mark) >= 0).length === a1.length;
